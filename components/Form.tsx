import React from 'react'
import {images} from '../constants'

const Form = () => {
  return (
    <div>
        <h1>Lets build a form</h1>
<img src={images.logo.src} alt="" />
    </div>
  )
}

export default Form